# Protected zones from CLAUDE.md's "Internal Engineering Agent — process
# rules" section (EC-202/EC-400, PBSMS_Internal_Engineering_Agent_v1_1.pdf).
# Enforced as of 2026-08-24: this repo was made public (decision made and
# approved by the repo owner) specifically to unlock GitHub's "Require
# review from Code Owners" branch protection setting, which 403's on a
# private repo without GitHub Pro. Branch protection on `main` now requires
# a code-owner review from this file plus the required CI jobs green before
# any merge. `enforce_admins` is off, not on (corrected 2026-08-27 — see
# CLAUDE.md's merge-boundary section for why: the PR #1 self-approval
# deadlock, since GitHub won't let a PR author approve their own PR and
# this repo has only one collaborator).
#
# @SageCarter98 is currently the only collaborator on this repo, so the
# required-approval count is configured as 1, not EC-202's literal "two
# approvers, one Engineering Lead" — GitHub's review-count setting is
# per-branch, not conditional on which path changed, so it can't require
# 2 only for these zones. Revisit once a second engineer joins.
#
# EC-501 closed 2026-08-26: ci.yml's protected-test-integrity job fails a
# PR if an existing test case in any of the three files below was changed
# or deleted (a content hash, not just title, so a kept title with a
# gutted body still fails). Review of these files is still required
# regardless — this catches what a distracted reviewer misses, it doesn't
# replace the reviewer. `protected-test-integrity`, and `agent-pr-gates`
# (EC-500/502-506, ci.yml's other Agent-PR gate job), were both added to
# branch protection's required-status-checks list on 2026-08-27, under an
# explicit, one-time human-authorized exception to EC-205 (same category
# as the ci.yml edits themselves) — a red result on either now actually
# blocks a merge, not just shows as a failing check.

# Tenant isolation boundary
/pbsms-platform/infra/migrations/          @SageCarter98
/pbsms-platform/apps/api/src/common/tenant/    @SageCarter98
/pbsms-platform/apps/api/src/common/database/  @SageCarter98

# Authentication, authorisation, Chapter 13 scope resolution
/pbsms-platform/apps/api/src/modules/auth/     @SageCarter98
/pbsms-platform/apps/api/src/common/auth/      @SageCarter98

# Money correctness; maker-checker integrity
/pbsms-platform/apps/api/src/modules/finance/  @SageCarter98

# Academic record integrity; BECE and CSSPS depend on it
/pbsms-platform/apps/api/src/modules/results/  @SageCarter98

# Revenue correctness
/pbsms-platform/apps/api/src/modules/billing/  @SageCarter98

# Chapter 47 Tenant AI Assistant artefacts — prompts, scope configuration,
# retrieval rules, evaluation gates (EC-005, amended 2026-08-27; see
# CLAUDE.md). Needs a Chapter-47/AI-domain-owning approver too.
/pbsms-platform/apps/api/src/modules/tenant-ai-assistant/  @SageCarter98

# The only validation not downstream of the Agent's own understanding
# (EC-400) — never modify or delete an existing case in any of these four
# files. finance-invariants.e2e-spec.ts and results-immutability.e2e-spec.ts
# were added 2026-08-26, closing the documentation-drift gap where CLAUDE.md
# named both as already-protected suites before either file existed.
# tenant-ai-assistant-isolation.e2e-spec.ts was added 2026-08-27, the same
# EC-400 protection applied the moment it exists per CLAUDE.md's own
# pre-reserved slot for it.
/pbsms-platform/apps/api/test/tenant-isolation.e2e-spec.ts  @SageCarter98
/pbsms-platform/apps/api/test/finance-invariants.e2e-spec.ts  @SageCarter98
/pbsms-platform/apps/api/test/results-immutability.e2e-spec.ts  @SageCarter98
/pbsms-platform/apps/api/test/tenant-ai-assistant-isolation.e2e-spec.ts  @SageCarter98

# The rulebook itself, and CI configuration (EC-205)
/CLAUDE.md            @SageCarter98
/.github/workflows/   @SageCarter98
/.github/CODEOWNERS   @SageCarter98
