# NEXT_PUBLIC_API_BASE_URL: the NestJS API's real dev port (apps/api/src/main.ts
# defaults to 3001, via .env's PORT). Next.js's own dev server runs on its
# usual :3000 — see the root README's Quick Start.
NEXT_PUBLIC_API_BASE_URL=http://localhost:3001
