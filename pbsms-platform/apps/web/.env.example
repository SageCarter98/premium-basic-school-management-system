# NEXT_PUBLIC_API_BASE_URL: the NestJS API's real dev port (apps/api/src/main.ts
# defaults to 3000). Next.js's own dev server falls back to :3001 automatically
# when :3000 is already taken — see the root README's Quick Start.
NEXT_PUBLIC_API_BASE_URL=http://localhost:3000
