export default function DashboardPage() {
  return (
    <div>
      <h1>Dashboard</h1>
      <p style={{ color: 'var(--pb-ink-muted)' }}>
        Real dashboards (proprietor roll-up, headmaster approvals, accountant collections, teacher
        home) arrive in Stage 5 — this is the app shell proving it can host them.
      </p>
    </div>
  );
}
